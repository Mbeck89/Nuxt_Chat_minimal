export default defineAppConfig({
  ui: {
    primary: 'prime',
    gray: 'cool',
    // add custom colors here. Define them in tailwind.config

    ui: {
      icons: {
        dynamic: true
      }
    }
  }
})
